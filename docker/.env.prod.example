# Production Environment Variables
# IMPORTANT: Never commit this file with real credentials!

# PostgreSQL (REQUIRED - use strong password)
POSTGRES_DB=jasheets
POSTGRES_USER=jasheets
POSTGRES_PASSWORD=<STRONG_PASSWORD_HERE>

# Redis (REQUIRED)
REDIS_PASSWORD=<STRONG_PASSWORD_HERE>

# MinIO (REQUIRED for file storage)
MINIO_ROOT_USER=<MINIO_USER>
MINIO_ROOT_PASSWORD=<STRONG_PASSWORD_HERE>

# ChromaDB (REQUIRED for AI features)
CHROMA_AUTH_TOKEN=<SECURE_TOKEN_HERE>
